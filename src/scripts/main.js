/**
 * main.js — Global entry point.
 * Global JS logic only.
 * Block-specific logic lives in src/blocks/<block>/<block>.js
 * and is imported from page entry files (src/pages/<page>/index.js).
 */

// Example: auto-initialize block modules found on the current page
// import.meta.glob is available if needed for auto-discovery:
// const modules = import.meta.glob('../blocks/**/*.js');
